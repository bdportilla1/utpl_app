import 'package:flutter/material.dart';
import 'package:utpl_app/widgets/widgets.dart';

class PruebasScreen extends StatelessWidget {
   
  const PruebasScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: const AppBarWidget(titulo: 'PRUEBAS App'),
      //resizeToAvoidBottomInset: true,
      body: Column(
        

      )
    );
  }
}